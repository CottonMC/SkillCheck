package io.github.cottonmc.skillcheck.util;

public interface CharSheetClass {
  //TODO: do we still want the XP system? Also, what's a better name for this?
  int getNextLevelCost(int currentLevel);
}
